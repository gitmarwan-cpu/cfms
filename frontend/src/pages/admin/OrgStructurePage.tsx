import { useEffect, useState, type FormEvent } from 'react';
import { createOrgUnit, deactivateOrgUnit, fetchOrgUnits, fetchOrgUnitTypes, updateOrgUnit, type OrgUnit, type OrgUnitType } from '../../api/adminApi';
import type { ApiClientError } from '../../api/axiosClient';
import { ConfirmDialog, DataState, FormDialog, PageHeader } from '../../components/admin/AdminUi';
type Draft={name:string;code:string;orgUnitTypeId:string;parentId:string;phone:string;email:string;isActive:boolean};const empty:Draft={name:'',code:'',orgUnitTypeId:'',parentId:'',phone:'',email:'',isActive:true};
export default function OrgStructurePage(){const [units,setUnits]=useState<OrgUnit[]>([]),[types,setTypes]=useState<OrgUnitType[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState(''),[editing,setEditing]=useState<OrgUnit|null|undefined>(),[deleting,setDeleting]=useState<OrgUnit|null>(null),[draft,setDraft]=useState<Draft>(empty),[saving,setSaving]=useState(false),[formError,setFormError]=useState('');const load=()=>{setLoading(true);Promise.all([fetchOrgUnits(),fetchOrgUnitTypes()]).then(([u,t])=>{setUnits(u);setTypes(t)}).catch((e:ApiClientError)=>setError(e.message)).finally(()=>setLoading(false))};useEffect(load,[]);const open=(unit?:OrgUnit)=>{setEditing(unit??null);setFormError('');setDraft(unit?{name:unit.name,code:unit.code||'',orgUnitTypeId:String(unit.orgUnitTypeId),parentId:unit.parentId?String(unit.parentId):'',phone:'',email:'',isActive:unit.isActive}:empty)};const submit=async(e:FormEvent)=>{e.preventDefault();setSaving(true);const p={name:draft.name,code:draft.code||undefined,orgUnitTypeId:Number(draft.orgUnitTypeId),parentId:draft.parentId?Number(draft.parentId):null,phone:draft.phone||undefined,email:draft.email||undefined,isActive:draft.isActive};try{if(editing)await updateOrgUnit(editing.id,p);else await createOrgUnit(p);setEditing(undefined);load()}catch(err){setFormError((err as ApiClientError).message)}finally{setSaving(false)}};return <><PageHeader title="الهيكل التنظيمي" description="الوحدات التنظيمية وعلاقاتها الهرمية ضمن المؤسسة." actions={<button className="btn btn-primary" onClick={()=>open()}>إضافة وحدة</button>}/><DataState loading={loading} error={error} empty={!units.length} onRetry={load}><div className="card admin-tree">{units.map(unit=><div className="admin-tree-row" key={unit.id} style={{marginInlineStart:`${Math.min(units.filter(x=>x.id===unit.parentId).length?1:0,1)*24}px`}}><span><strong>{unit.name}</strong><small>{unit.unitType?.nameAr||'—'} · {unit.code||'بلا رمز'}</small></span><span className="admin-actions"><span className={`admin-status-pill ${unit.isActive?'is-success':''}`}>{unit.isActive?'نشطة':'معطلة'}</span><button className="btn btn-outline" onClick={()=>open(unit)}>تعديل</button>{unit.isActive&&<button className="admin-text-button danger" onClick={()=>setDeleting(unit)}>إلغاء التفعيل</button>}</span></div>)}</div></DataState>{editing!==undefined&&<FormDialog title={editing?'تعديل وحدة':'إضافة وحدة'} onClose={()=>setEditing(undefined)} onSubmit={submit} saving={saving} error={formError}><div className="admin-form-grid"><label className="field">الاسم<input value={draft.name} onChange={e=>setDraft({...draft,name:e.target.value})} required/></label><label className="field">الرمز<input dir="ltr" value={draft.code} onChange={e=>setDraft({...draft,code:e.target.value})}/></label><label className="field">نوع الوحدة<select value={draft.orgUnitTypeId} onChange={e=>setDraft({...draft,orgUnitTypeId:e.target.value})} required><option value="">اختر النوع</option>{types.filter(t=>t.isActive).map(t=><option key={t.id} value={t.id}>{t.nameAr}</option>)}</select></label><label className="field">الوحدة الأم<select value={draft.parentId} onChange={e=>setDraft({...draft,parentId:e.target.value})}><option value="">لا يوجد</option>{units.filter(u=>u.id!==editing?.id&&u.isActive).map(u=><option key={u.id} value={u.id}>{u.name}</option>)}</select></label><label className="field">الهاتف<input dir="ltr" value={draft.phone} onChange={e=>setDraft({...draft,phone:e.target.value})}/></label><label className="field">البريد الإلكتروني<input type="email" dir="ltr" value={draft.email} onChange={e=>setDraft({...draft,email:e.target.value})}/></label><label className="admin-check"><input type="checkbox" checked={draft.isActive} onChange={e=>setDraft({...draft,isActive:e.target.checked})}/> نشطة</label></div></FormDialog>}{deleting&&<ConfirmDialog title="إلغاء تفعيل الوحدة" message={`سيتم إلغاء تفعيل «${deleting.name}».`} onClose={()=>setDeleting(null)} onConfirm={async()=>{try{await deactivateOrgUnit(deleting.id);setDeleting(null);load()}catch(e){setError((e as ApiClientError).message);setDeleting(null)}}}/>}</>}
